<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Базовая тема приложения -->
    <style name="Theme.KuzyaBimGames" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Цвет статус-бара -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- Цвет навигационной панели -->
        <item name="android:navigationBarColor">@color/white</item>
        <!-- Иконки статус-бара тёмные -->
        <item name="android:windowLightStatusBar">true</item>
    </style>
    
    <!-- Тема для экрана-заставки -->
    <style name="Theme.KuzyaBimGames.Splash" parent="Theme.KuzyaBimGames">
        <item name="android:windowBackground">@drawable/background_main</item>
    </style>
</resources>
